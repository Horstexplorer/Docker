# Global defaults for all servers
{
  servers {
    # metrics
    strict_sni_host on
    trusted_proxies static private_ranges
    trusted_proxies cloudflare
    trusted_proxies_strict
  }
}

# Default abort for unhandled requests on ports 80 and 443
:80, :443 {
  abort
}